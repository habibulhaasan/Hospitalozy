{
  "name": "hospitalozy-functions",
  "description": "Cloud Functions for Firebase — admin-only employee account actions (see employees.js)",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true
}
